# Development environment configuration
# Updated to match the new OpenAPI specification

# Backend API URL - Updated to port 3001 as per new OpenAPI spec
VITE_APP_BACKEND_API_URL="http://localhost:3001"

# Alternative backend URL (if needed)
VITE_APP_BACKEND_API_URL_ALT="http://localhost:3000"

# Production URL (commented for development)
# VITE_APP_BACKEND_API_URL="https://api.senda.com"

# Image URL (keeping existing for compatibility)
VITE_APP_BACKEND_IMAGE_URL="https://app.senda.ventures/api/v1/image"
